/** Release metadata shared with the vendored Desktop Host. */

/** Lifecycle protocol generation implemented by the current upstream Host. */
export const DESKTOP_HOST_PROTOCOL_VERSION = 4 as const

/** Exact Harness release paired with this App build. */
export const EXPECTED_DSH_VERSION = '0.1.6-alpha.2'
