/** Maximum silence accepted from a model stream before the Studio reports failure. */
export const MODEL_STREAM_IDLE_TIMEOUT_MS = 30_000

/** Maximum wait for Harness to publish a requested permission preset. */
export const PERMISSION_PROJECTION_TIMEOUT_MS = 10_000
